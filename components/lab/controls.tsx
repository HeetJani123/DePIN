"use client";
import {Info} from 'lucide-react';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
import {metricDefinitions,formatMetric as fmt} from '@/lib/simulation/metrics';
export function Choice({label,value,options,onChange}:{label:string;value:string;options:string[];onChange:(v:string)=>void}){return <label className="field"><span>{label}</span><Select value={value} onValueChange={onChange}><SelectTrigger className="w-full" aria-label={label}><SelectValue/></SelectTrigger><SelectContent>{options.map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select></label>}
export function NumberField({label,value,onChange,min=0,max=100,step=1}:{label:string;value:number;onChange:(n:number)=>void;min?:number;max?:number;step?:number}){return <label className="field"><span>{label}</span><input type="number" value={value} min={min} max={max} step={step} onChange={e=>{const n=Number(e.target.value);if(Number.isFinite(n))onChange(Math.min(max,Math.max(min,step===1?Math.round(n):n)))}}/></label>}
export function Metric({name,value,note}:{name:string;value:number|null;note?:string}){return <div className="metric" data-metric={name} title={metricDefinitions[name].definition}><span className="metric-code">{name.slice(0,3).toUpperCase()}</span><div>{metricDefinitions[name].label}<Info size={13}/></div><strong>{fmt(name,value)}{metricDefinitions[name].unit==='cr'&&<em>cr</em>}</strong><span>{note||metricDefinitions[name].definition.split('.')[0]}</span><i className="metric-trace" aria-hidden="true"/></div>}
